# Strict review launcher request

This parent only proves its own read-only filesystem permission, delegates once, waits, and relays the final verdict. The reviewer owns all code/evidence/hash checks. Do not duplicate its review or repackage the long task. Use astra_reviewer and fork_turns="none", without model/effort overrides. The external watchdog is 240 seconds; delegate promptly and return immediately when the reviewer finishes.

Parent probe: hash index.ts, attempt os.open(path, os.O_WRONLY) without O_TRUNC/create/write, close if it unexpectedly succeeds, re-hash, and report actual errno plus covering /proc/self/mountinfo mount. No file edits. No /tmp, tests, new paid model probes or extra agents. The outer launcher already logged slot audit/status and runs in slot cpu. Do not modify configuration. If the required role cannot load, return BLOCKED with its actual error.

Pass this concise, self-contained TaskSpec to the child:

Goal: Independently review the current implemented changes in /home/tcuni-claw/pi/pi-planner-only against baseline 85bdd2a93b994d3e4894e7534ab91cf6b16c9043. Read the frozen complete ReviewRequest at .scratch/stability-next-20260920/continuation-20260920-0502/strict-ReviewRequest.md (SHA-256 c9ad0715e0deb049d901738781d6b43f03cc7a01cd4374c497aba4bbe9987495); it supplies the original contract, implementation report, all scoped files, neutral raw evidence and exact acceptance requirements. Independently prove your actual read-only enforcement; verify 66 source and 11 harness hashes; inspect changed production and direct paths, release and real provider/TUI evidence; recheck hashes. Earlier completed findings are linked in that request. Review only this scope and report uncovered limits honestly. No source/config/artifact edits, project tests, extra provider calls, credential reads, process kills, or child delegation. Never use /tmp; inherited TMPDIR is under /project/tmp. Do not start heavy work: this review is already inside the preflighted slot job. Return PASS / REQUEST_CHANGES / BLOCKED, findings with location/trigger/impact/evidence/minimum correction, runtime permission evidence and scope limits. Aim to finish within 150 seconds of starting so the parent can relay within its external 240-second deadline; return BLOCKED if required checks remain incomplete. Do not create a report file; return the result to the parent.
