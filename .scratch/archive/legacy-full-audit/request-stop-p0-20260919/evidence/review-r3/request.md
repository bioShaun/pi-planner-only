# Fresh review of the remaining typed launch failure correction

cwd /home/tcuni-claw/pi/pi-planner-only. Original approved P0 spec is ../../spec.md, full neutral implementation/scope/constraints are ../strict-review/request.md, and prior findings are ../strict-review/behavioral-review.md then ../review-r2/behavioral-review.md. Baseline3991c5c762584cbbc235576359734259f69931ce. This directory's source.sha256 is the current63-file frozen manifest; earlier manifests are historical. Root remains sole writer for task artifacts only.

Bounded delta since the second review: request-events.ts adds `t.reason === "launch_failure"` to the existing contract:delegation:launch branch, after coded exception precedence. request-stop.test.mjs adds a real plugin→REQUEST/RESPONSE→ledger scenario of three failed terminals carrying full zero-use metadata (turns0,durationMs0,toolCalls0 and zero tokens/cost), asserts the program stamps launch_failure, closes on that family, and admits no fourth launch. Existing nonzero/unknown-usage provider failed cases remain transient. No limits, retry policy or other source behavior changed. The pending resume guard from the earlier correction remains intact.

Current Root checks all pass: checks.json with raw check-1..4 stdout/stderr shows typecheck, expanded request-stop matrix, post-correction real SDK0.85.1/faux probe and whitespace. The final complete fixture usage metadata was rechecked in check-2-final.json/stdout/stderr. SDK raw results/trace are ../../request-host-run-mZtnv0/ (15 calls total,1 additional queued call after closure,1 child only in independent fresh Request,2 repairs with2 retained failures). request-control guard evidence remains ../review-r2/check-2.stdout; no controller edits since that check. Earlier independent validation summaries/raw logs are ../final-validation/ and ../final-validation-r2/.

Review the remaining typed-reason finding and adjacent precedence/behavior regressions against actual source and tests; verify current hashes. This is a fresh READ-ONLY BEHAVIORAL review, NOT strict isolation or release approval. Return findings with file/line/evidence/impact/minimal correction, code verdict and explicit gate limitations. Do not repeat the already closed broad review unless new evidence warrants it. No implementation history is provided beyond reports and prior findings.

Do not edit files or produce artifacts, spawn agents, execute tests, use /tmp, perform external effects, change configuration or attempt permission probes. Use supplied raw executable evidence and short readonly reads/hashes/git checks. Commands >1minute/>2GB/heavy /data_0 require logged preflight+slot; do not start them. AGENTS requires normal terminal/CI for release and subprocess tests; never reroute them. Strict parent launch was blocked by shared slot path/socket permissions (../strict-review/blocked.json, exit255/events0); in-session review does not replace it. Full release/real CLI/TUI/0.69 launcher and natural-language/default calibration remain outstanding. Return final report by message; Root will save it.
