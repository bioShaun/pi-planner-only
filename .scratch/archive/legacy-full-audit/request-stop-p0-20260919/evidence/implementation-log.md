# Implementation log

2026-09-20: Approved spec/defaults/ticket split. Ticket 04 implemented by astra_worker; writing stopped, Root now sole writer for 01–03/integration.

Correction to historical discovery scope: real plugin fingerprint collection calls execFileSync(git rev-parse HEAD). Old plugin probes were not entirely subprocess-free. Raw historical probes/results remain unchanged. New fixture replaces this Git boundary explicitly; protected subprocess and release probes remain ordinary-terminal-only.

Root implementation: request-control.ts owns finite request limits, synchronous durable dispatch claims, no-progress families/causal resolution, lifecycle and diagnostics. request-events.ts translates typed Task/host facts. index.ts gates public hooks, structured assistant calls, registered execute and final dispatch; command resume requires human UI confirmation because public command contexts have no provenance. task.ts/delegate.ts/ledger-store.ts commit the existing per-Task revalidation grant once at dispatch. CONTEXT/ADR/READMEs document ownership and capabilities. Legacy index/policy fixtures use explicit high operator quotas to keep independent policy cases within one fixture; their subprocess probes are unchanged and were not executed here.

Focused self-checks passed for request-control, request-stop, revalidation-accounting, typecheck and diff whitespace. Real SDK 0.85.1 probe runs preserve raw results/trace under request-host-run-*; the first settlement occurs after 3 failing calls plus 1 queued model call, with 0 child launches after closure. A later idle interactive input starts a new request and launches one child. Unknown tools rejected before tool_call also count through structured message_end observations. These are faux provider/fake Git/fake child-source results, not real launcher/CLI/TUI acceptance.

Failed development attempts are retained in conversation tool output and existing run directories. Initial RequestClosed parameter properties were incompatible with Node strip-only execution and changed to explicit properties. A fixture with pending fake children exited 13 because unref timers did not keep Node alive; a bounded harness watchdog corrected that. The SDK probe initially allowed no tools and therefore observed no hooks; its explicit registered-tool list corrected the harness. Subsequent queued denied results duplicated failure members after closure; closed records now ignore new failure observations while still recording stop facts. A report-correction test initially sent version 99 through a transport that assumes launcher validation and expected report-invalid; it now uses a Task identity mismatch to exercise the existing changes_requested/report_correction path. A follow-up assertion incorrectly treated the public details.decision string as an object; corrected. No protected probe was weakened to accommodate sandbox EPERM.

Final integration refinements: reviewer findings bind to the worker report execution so later accepted corrections resolve the right causal members; a verified new request clears the legacy exact-refusal breaker; session identity changes close both admissions even without a session_start event; diagnostics include committed/emitted/terminal facts and unresolved stops retained in request history. Root remains sole writer until the independent validation handoff.

Independent validation round 1 (final-validation/): 21/22 commands passed, source hashes unchanged. The revalidation fixture intentionally replayed a settled tool ID (correctly closing Request) then expected subsequent calls to proceed. Root corrected this fixture to drive agent_settled + a new interactive input and added the Task-local replay assertion across that request boundary; no production protection was weakened. The original failure logs remain immutable.

Bounded follow-up: structured message_end observations now retain registered-tool schema issues and stable argument failure families when host validation rejects before tool_call. The real SDK probe demonstrates two actual field reductions consume exactly two repair credits while retaining both failures. An initial numeric-value probe was coerced to strings by the SDK and reached TASK_UNKNOWN, so it was replaced by non-coercible object values; its failed trace/state remain under request-host-run-p1dYoz. Root self-checks passed revalidation-accounting and the updated real SDK probe (request-host-run-nQFLln: 15 total faux model calls; one additional queued call after closure; only the subsequent independent Request launched a child).

Independent validation round 2 (final-validation-r2/): all seven affected checks PASS, source hashes unchanged; original unmodified pure-test passes remain in round1. SDK raw results/trace are request-host-run-X60LCM. The raw started-utc.txt is authoritative (00:19:23Z); the report prose is one second early. Writer stopped and returned ownership to Root.

Strict review attempt: neutral ReviewRequest, baseline tracked patch and complete source hashes frozen in strict-review/. slot audit/status captured before submission. Submission failed exit255 because the shared slot job/audit paths are read-only and cpu.socket cannot be connected to (Operation not permitted); events.jsonl is empty, so no independent readonly parent or child was launched and no runtime permission proof exists. Root did not bypass slot or change permissions/configuration. A separate in-session fresh astra_reviewer is performing behavioral code review; this is explicitly not a replacement strict gate. Complete release and real launcher/CLI/TUI tests remain ordinary-terminal-only under AGENTS.

Fresh behavioral review returned REQUEST_CHANGES with two findings (strict-review/behavioral-review.md): pending old invocations must block operator resume, and known provider terminal reasons must classify as transient rather than unknown. Root's bounded correction adds the pending-claim guard, keeps returned-but-stop-unconfirmed child holds distinct, maps existing provider/report/launch terminal facts to stable families, and adds regression coverage for pending resume and typed terminal failures. Source from the first review remains recorded in strict-review/source.sha256; corrected source will be frozen separately.

First corrective checks (review-r2/) passed typecheck, request-control and whitespace; the expanded event-adapter matrix exceeded the old 15-second aggregate fixture watchdog. Its raw failure remains in checks.json. The suite-only watchdog was raised to 30 seconds to cover the added production quiet-sampling delays; the retained retry passed in 18.4 seconds. No product deadline or quota changed.

Second fresh behavioral review (review-r2/behavioral-review.md) closed the pending resume and provider/report findings but found the existing typed launch_failure reason still fell into unknown-failure. Root's second bounded correction maps that reason to contract:delegation:launch after coded exception precedence. The real plugin/event/ledger matrix now also covers three failed terminals with complete zero-use metadata, the program's launch_failure reason, closure, and no fourth REQUEST. request-stop passed after completing the fake usage contract with toolCalls:0 (review-r3/check-2-final.json; 19.55 seconds).

Final Root checks (review-r3/checks.json) passed typecheck, request-stop, post-correction SDK probe and whitespace. SDK run request-host-run-mZtnv0 reports 15 total faux model calls, one queued call after closure, zero children in the closed request, one child only after independent interactive input, and two repair credits with two failures retained. These are self-checks; the previous independent validation evidence remains separately attributed.

Third fresh behavioral reviewer (review_request_p0_launch_fix, fork_turns=none) returned PASS for the bounded code correction, no actionable findings, and verified all 63 final frozen files. Its exact substantive report is saved in review-r3/behavioral-review.md. Full acceptance is still BLOCKED by normal-terminal release/subprocess, real CLI/TUI/0.69.0 launcher and default calibration evidence, plus the failed strict launcher with no runtime isolation proof. No source edits followed that final review. Root prepared syntax-checked ordinary-terminal release and strict-review scripts without executing forbidden checks or retrying unchanged slot permissions. Final status and scope are collected in acceptance.md; source equality and workspace state are captured in review-r3/final-state.json before handoff.
