# Legacy full-audit campaign

These files target the pre-lite architecture (tag `legacy-full-audit`): TaskSpec/WorkerReport, the Task ledger, evidence, closeout, request control, and the Worker Runaway Controller.

This directory is history, not an active queue. Pick up work from a feature directory directly under `.scratch/` (see `docs/agents/issue-tracker.md`). A `Status:` line in this tree, including `ready-for-agent` or `needs-triage`, is the label from that campaign and must not be picked up.
