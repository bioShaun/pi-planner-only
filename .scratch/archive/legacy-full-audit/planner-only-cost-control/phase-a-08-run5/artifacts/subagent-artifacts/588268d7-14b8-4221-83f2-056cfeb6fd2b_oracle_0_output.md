The listed validation commands were re-run successfully. Output below.

**Inherited decisions:**
- This is a planner-only oracle re-run (ORACLE_SUITE=full) for the `/planner-only` Pi extension in this repo; the validation commands are read-only `grep` inspections of `index.ts` and `index.test.mjs` — no test-suite execution was requested, so no slot/heavy-task rules apply.

**Validation results:**

`grep -n "status" index.ts | head -40` — found (4 hits):
- L54: WorkerReport contract mentions `status (completed|partial|blocked|failed)` in the worker prompt text.
- L61: review policy line — oracle gates validation on `HEAD/status + named tests`, with full suite only when `PI_PLANNER_ONLY_ORACLE=full` (matches this run's `ORACLE_SUITE=full`, but note the test suite itself was not invoked here, only greps).
- L558: planner prompt instructs `git_audit` verification of `(status, diff, HEAD, log)`.
- L903–1102: `/planner-only` command dispatcher — `status` is the default action; usage string lists `[status|on|off|task...|review|usage]`.

`grep -n "status" index.test.mjs | head -40` — found (head cut at 40; hits span L114–1285):
- L114/124: expected tool calls include `git status --short` and `git_audit` status op.
- L253–392: probe exit-status assertions (RF release-gate checks: skipped contract suite and out-of-range peer must fail the gate).
- L400–621: RF-4 tests — D1/D2 per-session force-on, three `status` handler invocations with env/marker/default sources.
- L641–1285: git mock `status --porcelain=v2 --branch` responses, WorkerReport `status: "completed"`, and canonical task-id assertions (CANON100/CANON200/CANON206).

**Diagnosis:** Source and tests are consistent on the `status` contract: the command dispatcher defaults to `status`, tests exercise that default path plus the three RF-4 status-source variants, and the release gate assertions remain wired. No conflict between source behavior and test expectations surfaced from these greps.

**Drift check:** None visible. The env var named in source (`PI_PLANNER_ONLY_ORACLE=full`) aligns with this run's declared `ORACLE_SUITE=full` intent. One observation: line 61 says the full suite runs "only if PI_PLANNER_ONLY_ORACLE=full" — if the main agent intends the full test suite to execute as part of this rerun, the env var must actually be set in the shell that runs `npm test`; the two grep commands alone do not trigger it.

**Recommendation:** Accept these grep results as the oracle evidence. If the parent wants the full oracle suite (test execution, not just greps), that requires setting `PI_PLANNER_ONLY_ORACLE=full` and running the suite — say so and I'll run it (read-only inspection of repo state shows nothing blocking).

**Risks:** The 40-line `head` truncation hides any `status` hits beyond L1285 in `index.test.mjs`; if the parent needs exhaustive coverage rather than the commanded 40-line window, I can re-run without truncation.

**Need from main agent:** None — the commanded greps are complete. Only decision outstanding is whether "full suite" means actually executing `npm test` with `PI_PLANNER_ONLY_ORACLE=full` set.

**Suggested execution prompt:** None — no worker handoff warranted; this was a read-only validation re-run and it passed as specified.