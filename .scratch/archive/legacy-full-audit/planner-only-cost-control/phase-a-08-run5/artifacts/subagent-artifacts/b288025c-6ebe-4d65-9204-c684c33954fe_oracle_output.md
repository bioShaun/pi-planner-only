Findings (recon only, no files edited):

**Validation commands run:**
```
grep -n "status" index.ts | head -40        → output captured above
grep -n "status" index.test.mjs | head -40  → output captured above
```

**Status command implementation — `index.ts`, lines 906–921** (the `status` action builds its output lines here):

```
906  if (action === "status") {
907    const log = usageLogPath();
908    const logStatus = log ? `${log} (enabled)` : "disabled";
909    const lines = [
910      `Planner-only mode is ${isDisabled() ? "off" : "on"} (source: ${guardDecisionSource()}).`,
911    ];
912    const forcing = envForcingValue();
913    if (forcing !== undefined) {
914      lines.push(`Environment: PI_PLANNER_ONLY=${forcing} forces planner-only ${envForcesGuard() ? "on" : "off"}.`);
915    } else if (existsSync(OFF_MARKER)) {
916      lines.push(`Marker: ${OFF_MARKER}`);
917    }
918    lines.push(`Usage log: ${logStatus}`);
919    notify(ctx, lines.join("\n"));
920    return;
921  }
```

Dispatch context: action defaults to `"status"` at line 903 (`(parts[0] ?? "status").toLowerCase()`), inside the `planner-only` command registered at line 899. Usage string at line 1102. Note: `const store = orchestrator.store;` (line 904) is captured before the status branch but not used in it — it's used by later task-lifecycle actions.

**Existing status assertions — `index.test.mjs`, two blocks:**

Block 1 — RF-4 (D1/D2/default per-session force-on + status source reporting), lines 437–469, with env-forcing and marker setup around them:

```
437  // D1: marker present + PI_PLANNER_ONLY=1 -> tools restricted, status source: env
438  process.env.PI_PLANNER_ONLY = "1";
439  writeFileSync(markerPath, "Disabled\\n");
440  const d1 = makePi(["read", "bash", "subagent", "write"]);
441  plannerOnly(d1.pi);
442  const notices1 = [];
443  const ctx1 = { hasUI: true, ui: { notify(msg) { notices1.push(msg); }, ... } };
444  await d1.handlers.get("session_start")({}, ctx1);
445  assert.deepEqual(d1.getActive(), ["read", "subagent"]);
446  await d1.commands.get("planner-only").handler("status", ctx1);
447  assert.match(notices1.at(-1), /Planner-only mode is on \\(source: env\\)/);
448
449  // D2: marker present + env unset -> tools unrestricted, status source: marker
450  delete process.env.PI_PLANNER_ONLY;
451  const d2 = makePi(["read", "bash", "subagent", "write"]);
452  plannerOnly(d2.pi);
453  const notices2 = [];
454  const ctx2 = { ... };
455  await d2.handlers.get("session_start")({}, ctx2);
456  assert.deepEqual(d2.getActive(), ["read", "bash", "subagent", "write"]);
457  await d2.commands.get("planner-only").handler("status", ctx2);
458  assert.match(notices2.at(-1), /Planner-only mode is off \\(source: marker\\)/);
459
460  // Default: no marker + env unset -> tools restricted, status source: default
461  rmSync(markerPath);
462  const d3 = makePi(["read", "bash", "subagent", "write"]);
463  plannerOnly(d3.pi);
464  const notices3 = [];
465  const ctx3 = { ... };
466  await d3.handlers.get("session_start")({}, ctx3);
467  assert.deepEqual(d3.getActive(), ["read", "subagent"]);
468  await d3.commands.get("planner-only").handler("status", ctx3);
469  assert.match(notices3.at(-1), /Planner-only mode is on \\(source: default\\)/);
```

Block 2 — T05b headless status (env-forced-off + marker source), lines 590–609 (surrounding ~15 quoted at 590–609 above; assertions at 596–607):

```
596  // headless status names the effective state and its source
597  await commands.get("planner-only").handler("status", ctx);
598  assert.match(sent.at(-1).content, /Planner-only mode is off \\(source: env\\)/);
599  assert.match(sent.at(-1).content, /PI_PLANNER_ONLY=0 forces planner-only off/);
600
601  // marker source is reported headless too
602  delete process.env.PI_PLANNER_ONLY;
603  const markerPath = join(process.env.PI_CODING_AGENT_DIR, "planner-only.off");
604  writeFileSync(markerPath, "off\\n");
605  await commands.get("planner-only").handler("status", ctx);
606  assert.match(sent.at(-1).content, /Planner-only mode is off \\(source: marker\\)/);
607  assert.ok(existsSync(markerPath));
```

Other grep hits in `index.test.mjs` are unrelated to the status command: `git status --porcelain` mocks (lines 641, 707, 714, 898, 925, 929, 935, 982, 1081, 1087, 1217, 1223, 1285), child-process `.status` exit-code checks (253, 320, 342, 362, 380, 392, 482, 552, 621), and a WorkerReport `status: "completed"` fixture (line 721).

**Summary:** The `/planner-only status` output is built in `index.ts:906–921` — up to 4 lines: mode+source, environment forcing (or marker line), usage log, joined and passed to `notify(ctx, ...)`. Existing coverage asserts the mode/source line and env-forcing line via UI `notify` (RF-4, lines 446–469) and headless `sendMessage` content (T05b, lines 597–607). No assertion currently checks the `Usage log:` line or the `Marker: <path>` line directly. No executor handoff warranted for this recon task.