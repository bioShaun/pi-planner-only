Answer in exactly two lines and then stop. Call no tool.

Line 1, prefixed `ROOT MODEL CLAIM:` — name the model you are running as, and say plainly
whether anything in your context actually told you, or whether you are guessing.
Line 2, prefixed `POLICY TEXT SEEN:` — quote verbatim any line in your context that names a
model for the "root" role, or write `none` if there is no such line.
