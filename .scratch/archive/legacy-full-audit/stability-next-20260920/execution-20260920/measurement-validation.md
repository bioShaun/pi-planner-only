# Independent P3 evidence validation

Native `astra_validator_complex` returned PASS after read-only inspection and recalculation of `study-run-yL4RzS`. This is ordinary evidence validation of the measured pre-hardening build, not a strict code review.

- Exactly 27 rows preserve the predefined 3 arms × 3 tasks × 3 repetitions rotation. All process exits are 0, quality checks true, actual Gemini Root / Luna child / low identities verified.
- 128 raw Root usage records and 20 matched REQUEST/terminal child pairs produce 20 unique completed terminals, with no duplicate tuple or unmatched request. Independent sums equal 54,524 / 815,498 / 846,506 total tokens for direct / baseline / optimized, retaining all cache components.
- Independently sorted durations reproduce median 7,560 / 35,585 / 34,879ms, maximum 16,773 / 94,617 / 64,503ms, and totals 78,182 / 404,733 / 400,493ms. Measurement includes slot submission through host exit.
- Every monetaryCost is null. No pricing or savings claim is supported.
- All 27 final bodies, SHA-256 values and Git statuses in quality-audit.json match retained private workspaces. Read tasks leave clean worktrees; edits leave exactly value.json modified and bytes `{"enabled":true}\n`; answer suffixes match.
- All nine baseline runtime-registration controls alter only model/thinking. Builtin private settings pin the same child identity; baseline sources remain loaded from 85bdd2a and tool definitions are unchanged.
- The ordinary study wrapper ran 09:25:24–09:40:13 UTC, exit 0, with identical source and harness before/after hashes.

Version boundary: measured evidence.ts/orchestrate.ts/p1-delegation.test.mjs precede final declaration-reliability hardening. The validator independently confirmed all 20 requests lack toolBudget and all 18 delegated ledgers contain no reportOnly execution or undeclared/over-declared/missing finding. Thus P3 did not exercise the repaired error path and its hashes are not final production hashes. Exact measured source provenance is retained in pre-truth-hardening.patch/source.json and the run's own manifests. Current malformed-declaration behavior is separately covered by red/green plugin-entry regressions and the later full release.

There are no observed failed child terminals in this study, so nonzero failure retention is covered by aggregation tests, not claimed as empirically exercised here. One auxiliary Node-spawned git read encountered the documented sandbox EPERM; direct read-only git status checks subsequently verified all workspaces. No repository test was changed or counted as passing on EPERM, and the validator made no file changes.
