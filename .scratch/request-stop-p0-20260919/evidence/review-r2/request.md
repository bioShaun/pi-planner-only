# Fresh corrective code review

cwd /home/tcuni-claw/pi/pi-planner-only. Original user approval, P0 contract, implementation report, baseline3991c5c762584cbbc235576359734259f69931ce, project constraints and initial evidence are in ../strict-review/request.md. The original approved spec is ../../spec.md. Current frozen source is this directory's source.sha256 (supersedes prior hash manifest for this round).

Prior fresh reviewer findings are preserved at ../strict-review/behavioral-review.md: pending invocation claims must prevent operator resume; confirmed provider terminal reasons must not fall into unknown-failure. Review their closure and regressions independently, against actual current sources/callers and the approved spec. Do not infer correctness from Root's implementation report below.

Bounded correction report: request-control.ts resume now rejects !waitSettled claims. Invocations already returned after cancel grace may still have stop=unconfirmed; those retain Writer holds and are a different condition. request-control.test.mjs first verifies resume denial/request identity retention with a pending restored claim, then records the waiter return, permits resume, verifies retained historical uncertainty, and finally binds the late confirmation to the old request. request-events.ts maps provider_failure to transient, structured_output_failed/acceptance_failed to contract:report, invalid_request/duplicate_node to contract:delegation:launch; existing stop uncertainty, environment, budget/cancel and coded exception precedence remains. request-stop.test.mjs drives three real event-adapter terminal sequences into the family cap and verifies no fourth launch. No automatic retry or limit changes were introduced.

Current correction checks by Root: checks.json plus check-1..4 stdout/stderr; typecheck, request-control and whitespace passed. Initial expanded request-stop suite hit the old15s aggregate test watchdog after both new provider/report sequences had already passed; raw failure is retained. A third launch-status sequence was added, and the suite watchdog became30s to allow the production250ms quiescence sampling gaps across the full matrix. Retry raw result check-3-retry.json/stdout/stderr passed in18.4s. Product request deadline and cancellation assertions remain unchanged. Earlier independent bounded verification remains ../final-validation-r2/summary.md (7/7) and original unaffected pure tests in ../final-validation/summary.md; the old failed21/22 run remains immutable. SDK host evidence is still ../../request-host-run-X60LCM/results.json and trace.json; SDK was not rerun after these two narrow corrections.

Scope: fresh in-session READ-ONLY BEHAVIORAL corrective review, not strict runtime isolation. Verify current frozen hashes, pending/returned invocation distinction, terminal taxonomy including precedence, actual tests and adjacent affected paths. Return code findings severity/location/trigger/impact/minimal fix, independent checks and limitations, with PASS/REQUEST_CHANGES/BLOCKED appropriate to scope. Report separately whether code findings remain and whether full release gates are available.

The required independent readonly parent never launched: slot submission exit255, jobs/audit paths read-only and cpu.socket denied, events0bytes (../strict-review/blocked.json and launcher.stderr). Do not repeat this blocked launch or substitute role metadata for proof. No runtime write probes in this behavioral review. Full release, child-process tests, real CLI/TUI/launcher0.69, natural-language acceptance and representative default calibration remain unavailable.

Do not write any files, spawn agents, run subprocess-producing tests/npm test/test:release, contact external systems, use private host APIs or change configuration. Do not use /tmp. Root is sole writer only of task progress artifacts outside frozen scope; source remains frozen. Read/hash/git diff checks are allowed. Use existing raw executable evidence; no repeat tests or commands over1minute/>2GB/heavy /data_0. Return final report in message only; Root will save it. Global astra protocol applies, with a new reviewer and fork_turns=none.
