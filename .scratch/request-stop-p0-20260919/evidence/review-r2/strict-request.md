# Independent review request: P0 durable Request admission

Project cwd: /home/tcuni-claw/pi/pi-planner-only. Review the current frozen files, including untracked new modules/tests, against baseline 3991c5c762584cbbc235576359734259f69931ce. User approved the defaults and five-ticket split: “采用建议值和拆分，继续实现”. Original contract: .scratch/request-stop-p0-20260919/spec.md and issues/01 through 05. P1–P3 and model routing changes are out of scope.

Implementation report (facts only): request-control.ts owns durable Request limits and admission, launch claims, failure chains, correction ancestry, deadlines, root/child stop observations and trusted request transitions. request-events.ts adapts structured Task/host facts. index.ts integrates hook, structured message, execute and final dispatch gates, typed failure observations, trusted interactive input and UI-confirmed operator resume. task.ts/delegate.ts/ledger-store.ts bind and persist the existing three revalidation attempts at dispatch, reject replay and conservatively retain uncertain sends. package.json includes new modules/tests. CONTEXT.md, README.md, README.zh-CN.md and docs/adr/0005-durable-request-admission.md describe the resulting contract. New tests are request-control.test.mjs, request-stop.test.mjs, revalidation-accounting.test.mjs. Existing index.test.mjs and policy-cutover.test.mjs fixtures were adapted to isolate their many independent cases from request quotas; their subprocess probes remain intact and unexecuted in this sandbox.

Defaults:32 tool attempts,8 child claims,3 unresolved failures per family,2 structural repairs,900000 ms absolute active deadline. PerTask revalidation3. Charges/closure persist across new Tasks, corrections, recovery, reload and automatic continuation. A next idle interactive input after settled, or actual operator UI confirmation, may open a new Request without releasing Writer holds. Only accepted sameTask/sameFamily causal ancestors can be resolved. Child send commits precede REQUEST; sending unknown never refunds a committed claim. Public host abort/terminate does not prove all-mode Root stop.

Frozen evidence in this directory: source.sha256 (include new files; verify against cwd), tracked.patch, status-before.txt, head.txt, frozen-at.txt. Earlier before-implementation evidence is ../implementation-before.txt and ../implementation-before.sha256. Source outside listed implementation scope and pre-existing untracked docs/pi-planner-only-stability-and-delegation-plan.md and .scratch/request-loop-verification-20260919/ was not changed by this work.

Independent bounded validation: ../final-validation/summary.md (initial21/22; raw failing revalidation fixture retained) and ../final-validation-r2/summary.md (all7 affected rechecks PASS, source hashes unchanged). Use raw started-utc.txt for authoritative timestamps. Per-command stdout/stderr/exit/UTC evidence is checks/ under each directory. Round1 also ran unchanged policy/roles/floors/role-models/ledger/report/review/usage/workspace/concurrency/refusal/architecture/naming pure tests and syntax-only checks of protected tests. Round2 SDK evidence is ../../request-host-run-X60LCM/results.json and trace.json: installed pi host0.85.1, real AgentSession/ExtensionRunner and plugin with faux model, fake Git and fake child event source;15 total model calls,1 extra queued model call after closure,0 child launches in closed requests,1 only after independent interactive request;schema repair consumes2 credits and retains2 failures. The new fixtures explicitly replace fingerprint Git execFileSync before plugin import. Historical earlier probes/results remain untouched.

Review requirements: independently inspect actual files/callers/state and test assertions, not just reports. Assess specification and correctness, preserve existing typed TaskSpec/report/identity/evidence/Writer hold boundaries. Findings need severity, exact file/line, trigger, evidence, consequence and minimal correction. Missing executable required acceptance is BLOCKED, not an invented product defect. Return PASS / REQUEST_CHANGES / BLOCKED with scope and limitations, and report substantive findings even if a gate is unavailable.

Required release/real-host evidence is absent: AGENTS.md prohibits npm run test:release and any test spawning child processes in sandboxed agent executors (including index, policy-cutover and task tests). Do not run or reroute these. Ordinary terminal/CI release, real CLI/TUI + pi-subagents0.69.0 transport and natural-language acceptance, representative default-limit calibration remain outstanding. No complete Root-stop/token/cost guarantee is claimed. Do not convert bounded faux results into real host acceptance.

Permissions and execution: strict gate uses independent readonly parent and astra_reviewer child with fork_turns=none, no implementation history, no further agents from the child. The external launcher has240s timeout plus10s grace. Actual parent/child runtime filesystem enforcement must be probed and evidenced; role TOML or behavioral refusal is insufficient. For an authorized denial probe only, attempt creating a unique parent-probe or child-probe file under this review-r2 directory; record actual tool/OS result and whether it exists. Do not modify source. Unexpected successful probe means strict isolation failure; leave that tiny artifact as evidence. Do not use external messages, paid host tests, network side effects, private APIs or configuration changes.

No /tmp use. TMPDIR/TMP/TEMP point at review-r2/tmp, whose parent was verified. Heavy commands (>1minute/>2GB/heavy /data_0) require slot after logging audit/status; do not start such extra commands here, change slot capacity or kill others. Review can read frozen evidence and perform short readonly checks. There is one cwd writer (outer Root solely for review logs); neither review parent nor child may write except the explicitly requested denial probes. Project referenced .codex/codex-subagent-config-astra-planner.md is absent; global /home/tcuni-claw/.codex/astra-planner.md applies. Role/config changes were not made.


## Corrective round evidence

This request targets the corrected source.sha256 in review-r2, not the earlier strict-review manifest. Prior behavioral review findings are ../strict-review/behavioral-review.md. Pending invocations now block operator resume; existing provider/report/launch terminal facts map to stable failure families. Root correction checks are checks.json plus check-3-retry.json and matching raw outputs; the first expanded test watchdog failure is retained and the corrected matrix passes in18.4seconds. Final in-session corrective review is behavioral-review.md in this directory when present; it is not strict isolation evidence. SDK evidence predates these narrow corrections. If ordinary-terminal release-run-* evidence has since been produced under ../../, independently inspect its source hashes and raw logs rather than assuming the frozen missing-evidence statement is still current. Full release and real-host acceptance must be established before final approval.
