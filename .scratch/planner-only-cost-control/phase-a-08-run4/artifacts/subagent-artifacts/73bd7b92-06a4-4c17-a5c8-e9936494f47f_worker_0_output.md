The task was already implemented and committed in a prior run on this branch; I verified the implementation matches the spec and re-ran full validation.

**Verification of the existing change (commit `4a72dd0` on `phase-a-08-rerun-4`):**
- `index.ts` imports `oracleSuiteMode` from `./roles.ts` (the same helper wrapping Validator prompts) and the `status` action pushes `Oracle suite: ${oracleSuiteMode()}` after the `Usage log:` line — default env → `bounded`, `PI_PLANNER_ONLY_ORACLE=full` → `full`.
- `index.test.mjs` (lines 1701–1705) asserts both values in the existing status block.
- No changes to default oracle behavior, Worker contracts, Reviewer packets, usage accounting, or README. Working tree clean, nothing staged, `.scratch/` untracked, no push.

**Validation:** `npm run typecheck` clean; full `npm test` — all 14 suites PASS (policy, roles, task, report, review, notify, usage, orchestration, evidence, git_audit, index/extension, workspace-snapshot, architecture, naming).