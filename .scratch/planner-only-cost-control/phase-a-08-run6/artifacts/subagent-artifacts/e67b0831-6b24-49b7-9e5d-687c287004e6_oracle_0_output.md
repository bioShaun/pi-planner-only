All validation re-runs pass. Final oracle report:

**Inherited decisions:**
- Task T-20260908-001: `/planner-only status` must print one extra line `Oracle suite: bounded|full`, sourced from `oracleSuiteMode()` in `roles.ts`, with tests added near existing U-5 status assertions in `index.test.mjs`.
- Acceptance contract: existing status lines unchanged; `node --experimental-strip-types index.test.mjs` exits 0; `npm run typecheck` exits 0; single commit on `phase-a-08-rerun-6`, no push, `.scratch/` left untracked.
- This is a re-run (r6) — prior decision was to keep the change minimal: one import, one `lines.push`, one test block.

**Diagnosis:**
- HEAD `fd786ad` "feat(status): print Oracle suite line from oracleSuiteMode()" is exactly the expected minimal diff: +1 import in `index.ts`, +1 `lines.push(`Oracle suite: ${oracleSuiteMode()}`)` after the `Usage log:` line (index.ts:920), +11 lines in `index.test.mjs` appended directly after the existing U-5 status assertions (lines 1701–1711).
- `oracleSuiteMode()` already existed in `roles.ts:51` with its own unit coverage in `roles.test.mjs:318-321` (bounded default, full/FULL/bounded env handling) — no new source function needed, consistent with the inherited scope.

**Drift / contradiction check:**
- None found. The new line is appended after `Usage log:`, so all pre-existing lines (mode/source, Environment/Marker, Usage log) are byte-identical. The test correctly saves/restores `PI_PLANNER_ONLY_ORACLE` around the mutation, so it won't leak env state into other tests in the same process.

**Validation results (re-run, observed this session):**
- `node --experimental-strip-types index.test.mjs` → `planner-only extension: PASS`, exit 0. Both new assertions (`Oracle suite: bounded` default, `Oracle suite: full` with `PI_PLANNER_ONLY_ORACLE=full`) execute and pass.
- `npm run typecheck` (`tsc --noEmit`) → exit 0.
- `git status`: only untracked `.agent-dir/`, `.scratch/oracle-status-line/`, `.scratch/phase-a-08-session/` — `.scratch/` left untracked as required; nothing staged or modified.
- Branch is `phase-a-08-rerun-6` with the single feature commit on top of the prior orchestration commit; no push performed (no remote ref ahead).

**Recommendation:** Accept — all acceptance criteria are met, no further work needed. No executor handoff is warranted.

**Risks:** Minor: test mutates `process.env` — the save/restore guard handles it, but a thrown assertion between set and restore would leak `PI_PLANNER_ONLY_ORACLE=full` into subsequent tests in the same file. Given Node's `assert` aborts the file on failure anyway, this is cosmetic. Also, the criterion was verified in-process (the status handler reads `process.env` at call time), not through a real CLI invocation — but the unit-level path is the same code the extension uses, so this is adequate.

**Need from main agent:** Nothing.