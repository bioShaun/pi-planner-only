fix(tools): bound all external subprocess calls with timeouts

Add ExternalToolTimeoutError, require positive timeouts for run and
version probes, and route replace-primaries annotate through ExternalTool
so hung children cannot block the pipeline indefinitely.

Co-authored-by: Cursor <cursoragent@cursor.com>

---

让以下测试通过且不破坏其他测试：tests/contracts/test_external_tool_timeout.py tests/contracts/test_prefilter_calibration_profile.py tests/unit/test_external_tool_version_reporting.py；测试命令：PYTHONPATH=src /public/scripts/tc-probe-design-v2/.venv/bin/python -m pytest <上述测试路径>（快速套件：同一命令不带路径参数）；不要修改这些测试文件；不要 commit。
