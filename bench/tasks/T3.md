fix(annotation): fail closed on explicit unusable BED evidence

Reject empty, missing, or malformed bed_dir inputs before probe-level
grading, while keeping legitimate no-overlap intergenic results and
recording the loaded region set for diagnostics.

Co-authored-by: Cursor <cursoragent@cursor.com>

---

让以下测试通过且不破坏其他测试：tests/contracts/test_bed_region_fail_closed.py tests/contracts/test_pyranges_annotation_contract.py tests/unit/test_annotate_region.py；测试命令：PYTHONPATH=src /public/scripts/tc-probe-design-v2/.venv/bin/python -m pytest <上述测试路径>（快速套件：同一命令不带路径参数）；不要修改这些测试文件；不要 commit。
