node_modules/
.planner-only-test-*/
.git-audit-test-*/
.planner-only-e2e-*/

.planner-only-*
.wl-debug-*

# herdr-pair round handoffs and per-round command logs
.handoff/
.scratch/planner-only-cost-control/**/*.log
.scratch/**/*.bak-*

# raw session transcripts from the phase-a-08 runs; the small evidence
# (meta.json, usage.jsonl, comparison.md) is committed, the logs are not
.scratch/planner-only-cost-control/phase-a-08-run*/artifacts/root-session.jsonl
.scratch/planner-only-cost-control/phase-a-08-run*/artifacts/subagent-artifacts/*_transcript.jsonl
.scratch/planner-only-cost-control/phase-a-08-run*/artifacts/session-tree/

# local leftovers (untracked dumps; p19 agent-dir contains auth.json)
.scratch/oracle-no-bash-repro/
.scratch/planner-only-cost-control/quarantine/p19-wt-leftover-r085-agent-dir/

# local pre-pull backup dumps
.scratch/.local-backup*/
